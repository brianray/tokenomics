# tokenomics

Template for deploying a self-contained static webpage to GitHub Pages.

## Quick start

1. Edit `/index.html` with your page content.
2. Push to `main`.
3. In GitHub, enable **Pages** and select **GitHub Actions** as the source.
4. The workflow in `.github/workflows/deploy-pages.yml` will deploy automatically.
